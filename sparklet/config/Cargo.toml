[package]
name = "config"
version = "0.1.0"
edition = "2024"

[dependencies]
amity = { version = "0.6.3", default-features = false, features = ["triple"] }
defmt = "1.0.1"

[dev-dependencies]
pretty_assertions = "1.4.1"
